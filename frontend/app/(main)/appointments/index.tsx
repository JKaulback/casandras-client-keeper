/**
 * Appointments List Screen - Fallback
 * This file is required by Expo Router as a fallback for platform-specific files
 * Actual implementations are in index.web.tsx and index.native.tsx
 */

// Export the native version as the default fallback
export { default } from './index.native';
